# Supabase — Project Settings → API
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...

# Production server (npm start)
PORT=4173
SITE_URL=https://bloxupgrader.com

# Persist chats, deposits, giveaways and grants across Render deploys
DATA_DIR=/var/data/bloxupgrader
# PROFILE_PHOTOS_DIR=/var/data/bloxupgrader/profile-photos

# Optional JSON backup table (SQL Editor):
# create table if not exists blox_json_state (
#   id text primary key,
#   payload jsonb not null,
#   updated_at bigint not null
# );

# Legacy (optional):
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
