# Promo codes (runtime data)

Deposit bonus codes are stored in `codes.json` at runtime. Create them from the admin **Promos** button in production.

Do not commit `codes.json` — each environment keeps its own codes.
