/** Faster live feed on localhost — bot tick, client poll, win rate for visible drops. */
export const DEV_FEED_BOT_TICK_MS = 900;
export const DEV_FEED_CLIENT_POLL_MS = 800;
export const DEV_FEED_WIN_RATE = 0.88;
